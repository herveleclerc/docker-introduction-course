title: docker-introduction
description: Docker for begginers
template:
  repo: docker-introduction-template
  name: docker-introduction
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
